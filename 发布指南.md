# 发布到 GitHub 指南（给作者）

## 一、仓库里应该上传哪些文件

✅ **要上传（源码 + 说明，干净、可移植）**

```
AutoCollect-TargetToggle/
├── ModEntry.cs                 # MelonMod 入口：目标玩家切换 + 按钮 + F9 + 配置
├── Framework/
│   └── BoardUpdatePatch.cs     # Harmony 补丁：扫描场上硬币并自动收集
├── AutoCollect.csproj          # 工程文件（引用路径已抽成 $(GameDir)，不再写死本机路径）
├── build.bat                  # 一键构建 + 部署（顶部 GAMEDIR 改成你自己的）
├── README.md                  # 给玩家看的使用说明（已改写为通用版）
├── LICENSE                    # MIT 许可证
├── .gitignore                 # 忽略 bin/ obj/ *.dll 等
└── 发布指南.md                # 本文件
```

✅ **建议额外在 GitHub Releases 里附上预编译 DLL**（给不懂编译的网友）

- 上传：`bin\Release\net6.0\AutoCollect.dll`
- 命名建议：`AutoCollect-v1.0.0.dll` 或在 Release 里直接叫 `AutoCollect.dll`
- 这样网友只需「下载 DLL → 丢进 Mods」即可，无需装 SDK

❌ **绝对不要上传（涉及版权 / 体积 / 隐私）**

| 不传的文件 | 原因 |
|---|---|
| `bin\`、`obj\` | 编译中间产物，且含本机路径 |
| `*.dll`（任何） | 含游戏本体 DLL（Assembly-CSharp 等）、MelonLoader、ReplantAPI —— 既有版权问题，也无法跨机复用 |
| `SAutoCollectMod.dll` / `ReplantAPI.dll` | 第三方 Mod，应让网友从原发布页（GameBanana）自行获取 |
| 游戏目录任何文件 | 版权 + 体积 |
| `build.log` | 本地日志，含路径隐私 |
| 你的 `UserData/`、`MelonLoader/` 配置 | 本地环境，无关 |

> 关键：`AutoCollect.csproj` 现在只通过 `$(GameDir)` 指向**你自己机器上** MelonLoader 生成的
> 引用 DLL，**这些 DLL 不进仓库**，网友克隆后用自己机器上的游戏路径构建即可。

---

## 二、发布步骤（命令行）

```bash
cd AutoCollect-Toggle

# 1) 初始化（如果还没建仓库）
git init
git add .
git commit -m "AutoCollect Toggle: 自动收阳光 + 玩家1/2 切换"

# 2) 在 GitHub 网页上新建一个空仓库（如 AutoCollect-Toggle），然后关联并推送
git remote add origin https://github.com/<你的用户名>/AutoCollect-Toggle.git
git branch -M main
git push -u origin main

# 3) 在 GitHub 仓库页 → Releases → Draft a new release
#    Tag: v1.0.0
#    把 bin\Release\net6.0\AutoCollect.dll 作为附件上传
#    正文贴 README 里的「安装」段落即可
```

如果你希望我**直接在本机帮你 init + commit**（push 需要你自己的 GitHub 账号授权，我无法代做），
告诉我即可，我可以执行到 `git push` 之前的所有步骤。

---

## 三、仓库描述 / 标签建议

- 仓库名：`AutoCollect-Toggle`
- Description：`PvZ Replanted MelonLoader mod: auto-collect sun/coins with switchable target player (P1/P2).`
- Topics：`pvz-replanted`, `melonloader`, `mod`, `unity-il2cpp`, `plants-vs-zombies`
